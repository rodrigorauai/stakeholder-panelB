{% extends 'base.html.twig' %}
{% set title = 'Nota Fiscal de ' ~ payment.account.owner.name %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

    <div class="container m-4">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-row button-container">
            {% include '_components/button/button.html.twig' with { label: 'Salvar Endereço da Nota Fiscal', type: 'submit', style: 'outlined'} only %}
        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}

    </div>

{% endblock %}